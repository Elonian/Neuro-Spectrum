training:
  device: auto
  batch_size: 0
  log_every: 100
  model_type: lstm
  lag: 96
  hidden: 64
  num_layers: 3
  epochs: 1200
  learning_rate: 0.02
  seed: 0

paths:
  training_data_dir: data/neural_training
  model_dir: outputs/models
  figures_dir: outputs/figures
